[project]
name = "stylevec"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "numpy>=1.26",
    "scikit-learn>=1.8",
    "requests",
    "huggingface-hub",
    "transformers",
    "torch",
    "accelerate",
    "peft>=0.10",
    "modelscope>=1.34.0",
]
